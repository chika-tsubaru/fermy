.content_row
  .main_quest 
    メインクエスト: 仮説力を向上させよう
  .quests_post
    - @quests.each do |quest|
      .content_row
        .content
          = quest.title
        .btns
          = link_to quest_path(quest.id), class: "content_btn" do
            解答集
          = link_to new_quest_answer_path(quest.id), class: "content_btn" do
            挑む